// Sync thresholds
// Above these thresholds, the sync toggle defaults to off with a recommendation to keep it off.
// The user can still enable it.

export const FOLLOWING_WARN_THRESHOLD = 1500;
export const FOLLOWERS_WARN_THRESHOLD = 3000;

// Tracking limits
export const MAX_TRACKED_ACCOUNTS_FREE = 1;
export const MAX_TRACKED_ACCOUNTS_PAID = 5;
